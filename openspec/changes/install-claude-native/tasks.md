## 1. Создать скилл dev:install

- [ ] 1.1 Создать `skills/dev/install.md` — скилл с логикой клонирования репо и копирования компонентов в `.claude/`
- [ ] 1.2 Поддержать аргументы: без аргументов (всё), `--dev` (включить `skill:`), именованные компоненты
- [ ] 1.3 Обновление `.versions` выполнять через Python-скрипт (структурированные данные по claude-way)
- [ ] 1.4 Зеркалить в `.claude/skills/dev/install.md`

## 2. Удалить scripts/

- [ ] 2.1 Удалить `scripts/install.sh`
- [ ] 2.2 Удалить директорию `scripts/`
- [ ] 2.3 Убрать `cp -r "$TMP/scripts" "$TARGET/"` из логики установки (скрипт больше не копируется пользователям)

## 3. Обновить README.md

- [ ] 3.1 Убрать секцию с `curl … | bash` из Quick Start
- [ ] 3.2 Заменить на инструкцию: открыть Claude Code и попросить установить awesome-claude
- [ ] 3.3 Обновить примеры выборочной установки и `--dev` режима под `dev:install`

## 4. Обновить спеку user-interface-principle

- [ ] 4.1 Запустить `sdd:apply` для применения delta-спеки `user-interface-principle` — убрать оговорку про `scripts/` как исключение

## 5. Зеркалирование

- [ ] 5.1 Зеркалить все изменения `skills/dev/` в `.claude/skills/dev/`
