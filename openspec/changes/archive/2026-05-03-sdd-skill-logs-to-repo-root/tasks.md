## 1. Gitignore

- [x] 1.1 Заменить `**/.log/` на `.logs/` в `.gitignore`

## 2. sdd:contradiction — смена пути лога

- [x] 2.1 Изменить путь `mkdir -p` и записи лога в `skills/sdd/contradiction/skill.md` с `openspec/changes/<name>/.log/` на `.logs/<name>/`
- [x] 2.2 Обновить строку-ссылку в финальном выводе contradiction: `→ Подробный технический отчёт: .logs/<name>/contradiction-<TS>.md`

## 3. sdd:propose — смена пути лога

- [x] 3.1 Изменить путь `mkdir -p` и записи лога в `skills/sdd/propose/skill.md` (шаг 12) с `openspec/changes/<name>/.log/` на `.logs/<name>/`
- [x] 3.2 Обновить строку-ссылку в финальном выводе propose

## 4. sdd:apply — добавить шаг лога

- [x] 4.1 Добавить финальный шаг записи лога в `skills/sdd/apply/skill.md`: `mkdir -p .logs/<name>` и запись `apply-<TS>.md`

## 5. sdd:archive — добавить шаг лога

- [x] 5.1 Добавить финальный шаг записи лога в `skills/sdd/archive/skill.md`: `mkdir -p .logs/<name>` и запись `archive-<TS>.md`

## 6. Обновление спеки

- [x] 6.1 Заархивировать delta из `openspec/changes/sdd-skill-logs-to-repo-root/specs/skill-run-log-archiving/spec.md` в `openspec/specs/skill-run-log-archiving/spec.md`
