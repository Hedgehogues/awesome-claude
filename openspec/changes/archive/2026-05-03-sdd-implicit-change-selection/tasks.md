## Tasks

- [x] В `skills/sdd/contradiction/skill.md` обновить шаг **Determine Change** (Mode B):
  - Убрать `git branch --show-current`, `git status --short` и ранжирование по трём сигналам.
  - Режим B: прочитать список директорий в `openspec/changes/`, отфильтровать архивированные (`.sdd-state.yaml` со `stage: archived`), показать плоский алфавитный список через `AskUserQuestion`.

- [x] В `skills/sdd/contradiction/skill.md` добавить явный блок SCOPE CONSTRAINT после Determine Change — текст блока согласно D3 в design.md.

- [x] В `skills/sdd/apply/skill.md` обновить шаг **Determine Change** (Mode B): убрать ранжирование, показывать плоский список активных ченджей.

- [x] В `skills/sdd/archive/skill.md` обновить шаг **Determine Change** (Mode B): убрать ранжирование, показывать плоский список активных ченджей.
