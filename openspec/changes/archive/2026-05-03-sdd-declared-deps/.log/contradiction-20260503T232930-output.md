## Описание

Проверены все артефакты ченджа: proposal, design, 3 спеки, tasks. Найдено одно противоречие: в design описан алгоритм ADJACENT-поиска как работающий по именам capabilities и первым 200 символам их описания, а спека того же алгоритма говорит, что матчинг идёт только по именам. Одно и то же поведение описано двумя разными способами — это нужно привести к единому виду. Дополнительно: формат вывода ADJACENT-секции продублирован в двух местах, а одна строка в tasks дублирует текст требования из спеки.

## Найденные противоречия

hard: 1, warnings: 2

## Что делать

- [ ] В design.md, раздел D3, убери «и первым 200 символам description» из заголовка — оставь только «по именам capabilities». Спека определяет алгоритм точнее: матчинг по имени, без description. (непонятно — скажи «поясни», слишком очевидно — скажи «детали»)
- [ ] В design.md, раздел D3, замени прозаическое описание формата вывода `--- ADJACENT Capabilities ---` на ссылку на спеку — чтобы не держать одно и то же в двух местах. (непонятно — скажи «поясни», слишком очевидно — скажи «детали»)
- [ ] В tasks.md, строка про «вывести warning и продолжить» в группе apply — замени подробный текст задачи на короткую отсылку к требованию `merges-into-missing-warning` из спеки, вместо того чтобы переписывать его заново. (непонятно — скажи «поясни», слишком очевидно — скажи «детали»)

→ Подробный технический отчёт: openspec/changes/sdd-declared-deps/.log/contradiction-20260503T232930.md

## Вопросы к пользователю

Нужно уточнить: алгоритм ADJACENT должен матчить только по именам capabilities, или всё же по именам плюс тексту их описания из index.yaml? Спека сейчас говорит «только имя», design в заголовке говорит «имя + 200 символов description». (непонятно — скажи «поясни», слишком очевидно — скажи «детали»)
